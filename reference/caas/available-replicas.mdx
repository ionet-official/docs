---
openapi: "/openapi/caas/available-replicas.json GET /enterprise/v1/io-cloud/caas/available-replicas"
title: "Check available replicas per location"
description: "Returns the number of deployable replicas in each location based on selected hardware type and GPU count per container, useful for matching supply to workload needs."
---

By leveraging this endpoint, it's possible to discover the current supply on the platform that fits workload requirements and use this data during the deployment process.