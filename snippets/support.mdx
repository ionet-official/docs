export const SupportBlocks = () => {
  const categories = [
    {
      name: "General",
      items: [
        {
          href: "/support/faq",
          title: "FAQ",
          description:
            "Find answers to common questions about using IO and its features.",
        },
        {
          href: "/support/staking",
          title: "Staking",
          description:
            "Information on staking $IO tokens, how to stake, and what rewards you can earn.",
        },
      ],
    },
    {
      name: "IO.Worker",
      items: [
        {
          href: "/support/block-rewards",
          title: "Block Rewards",
          description:
            "Learn how block rewards are distributed among workers and clusters.",
        },
        {
          href: "/support/worker-binary",
          title: "Worker Binary",
          description:
            "Guide for downloading and setting up the IO Worker for CPU and GPU devices.",
        },
        {
          href: "/support/troubleshooting",
          title: "Troubleshooting",
          description:
            "Solutions for common setup, connection, and performance issues with workers.",
        },
      ],
    },
  ];

  const Card = ({ href, title, description }) => {
    return (
      <a
        href={href}
        className={`
          group relative block rounded-2xl border p-7 min-h-[220px]
          transition-transform duration-200
          hover:-translate-y-0.5
          ring-0 ring-transparent hover:ring-1
          text-gray-800 dark:text-slate-100
          border-gray-200 dark:border-white/10
          bg-white dark:bg-black
          shadow-md dark:shadow-[inset_0_1px_0_rgba(255,255,255,0.03),0_10px_30px_rgba(0,0,0,0.35)]
          hover:shadow-lg dark:hover:shadow-[0_14px_38px_rgba(0,0,0,0.45),inset_0_1px_0_rgba(255,255,255,0.06)]
          hover:ring-gray-300 dark:hover:ring-white/10
        `}
      >
        <div className="mb-3 flex min-h-[32px] items-center gap-3">
          {title && (
            <span className="font-bold tracking-[0.08em] text-xs md:text-sm uppercase text-gray-900 dark:text-slate-50/95">
              {title}
            </span>
          )}
        </div>
        <p className="m-0 text-[15px] leading-6 text-gray-600 dark:text-slate-300/95">
          {description}
        </p>
      </a>
    );
  };

  return (
    <div className="mx-auto max-w-[1440px] px-4 lg:px-8 my-20">
      <div className="grid gap-16">
        {categories.map((cat, i) => (
          <div key={i}>
            <h2 className="mb-6 text-xl font-semibold text-gray-900 dark:text-slate-100">
              {cat.name}
            </h2>
            <div className="grid gap-7 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
              {cat.items.map((it, j) => (
                <Card key={j} {...it} />
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};
